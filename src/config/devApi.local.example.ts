/**
 * Copy to devApi.local.ts (gitignored) and set your Mac LAN IP for physical device testing.
 *
 * export const DEV_LAN_HOST = "192.168.1.42";
 */

export const DEV_LAN_HOST: string | null = null;
